name: Pipeline - Todo 2026 (mensual)

on:
  schedule:
    - cron: "0 7 1 * *"   # dia 1 de cada mes, 2:00am Peru (UTC-5)
  workflow_dispatch: {}

jobs:
  run:
    uses: ./.github/workflows/_pipeline-run.yml
    with:
      fase: bd
    secrets: inherit
