name: Pipeline - Reporte Diario

on:
  # PAUSADO: descomentar cuando esten listos los Secrets en GitHub
  # schedule:
  #   - cron: "0 15 * * *"   # 10:00am Peru (UTC-5)
  workflow_dispatch: {}

jobs:
  run:
    uses: ./.github/workflows/_pipeline-run.yml
    with:
      fase: reporte_diario
    secrets: inherit
