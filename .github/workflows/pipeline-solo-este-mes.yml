name: Pipeline - Solo este mes

on:
  # PAUSADO: descomentar cuando esten listos los Secrets en GitHub (mientras
  # tanto corre local via el scheduler de Intranet/app.py, con la PC prendida)
  # schedule:
  #   - cron: "0 * * * *"   # cada hora en punto (UTC)
  workflow_dispatch: {}

jobs:
  run:
    uses: ./.github/workflows/_pipeline-run.yml
    with:
      fase: daily
    secrets: inherit
